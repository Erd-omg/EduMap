# 智图 (EduMap) 前端高保真设计规格书

---

## 文档版本
- **版本**: v1.0
- **日期**: 2026-07-13
- **状态**: 设计定稿，可交付开发
- **对应PRD版本**: v2.0


## 第一部分：设计总览

### 1.1 设计原则

| 原则 | 说明 |
|---|---|
| **对话优先** | 所有功能始于对话，终于对话，统一交互入口 |
| **透明可解释** | 每个资源、每个建议都展示生成依据和置信度 |
| **渐进式披露** | 复杂功能分层呈现，不一次性堆砌信息 |
| **人机协同** | 用户可干预、可调整、可覆盖AI决策 |
| **高级而不疏离** | 莫兰迪色系营造专业感，同时保持温暖和亲和力 |

### 1.2 设计语言速览

| 属性 | 规格 |
|---|---|
| **色彩体系** | 暖灰莫兰迪基调 + 保留饱和度的功能色 |
| **布局架构** | 侧边栏导航（240px/56px）+ 可定制工作台 |
| **核心交互** | 流式输出、拖拽定制、悬停溯源、全屏模态 |
| **响应式策略** | 双断点（1024px / 640px），移动端底部Tab Bar |
| **字体** | Inter (中文回退 PingFang SC / Noto Sans SC) |


## 第二部分：色彩体系（莫兰迪色 Token）

### 2.1 基础色板

| 名称 | 色值 | 用途 |
|---|---|---|
| **背景基础** | #F5F2EB | 主背景 |
| **背景次要** | #E8E2D9 | 侧边栏、卡片背景 |
| **背景卡片** | #FDFCF8 | 浮层、模态框、卡片 |
| **分割线** | #DCD6CD | 边框和分割 |
| **文本主色** | #3D3A36 | 正文 |
| **文本次要** | #7A7368 | 辅助文字、占位符 |
| **文本浅色** | #B5ADA1 | 禁用文字 |

### 2.2 品牌主色

| 名称 | 色值 | 用途 |
|---|---|---|
| **主品牌色** | #A3B5A6 | 主要按钮、Logo、链接 |
| **主品牌悬停** | #8FA392 | 按钮悬停 |
| **强调色** | #C4A882 | 次要按钮、特殊标记 |

### 2.3 功能色（保留饱和度以确保可识别性）

| 名称 | 色值 | 用途 |
|---|---|---|
| **成功/掌握** | #4CAF50 | 技能树绿色节点、完成状态 |
| **进行中** | #FFC107 | 技能树黄色节点、进行中状态 |
| **待加强** | #EF5350 | 技能树红色节点、警告状态 |
| **信息** | #5B8DB8 | 提示信息 |
| **背景成功（莫兰迪化）** | #D4E8D4 | 绿色节点背景（低饱和） |
| **背景进行中（莫兰迪化）** | #F5E6C6 | 黄色节点背景（低饱和） |
| **背景待加强（莫兰迪化）** | #F0D4D4 | 红色节点背景（低饱和） |

### 2.4 多智能体专属色（按职能分冷暖）

| Agent | 角色 | 色值 | 应用场景 |
|---|---|---|---|
| **Orchestrator** | 编排者（无LLM） | #8FA3A8 | 任务看板日志前缀、Agent徽标 |
| **Planner** | 课程规划师（冷） | #7C8FA3 | 解析进度、知识点抽取 |
| **Guardian** | 知识库维护官（冷） | #8FA3B5 | 环检测、去重日志 |
| **Designer** | 多媒体内容官（暖） | #C4A89A | 文档生成、思维导图 |
| **Coder** | 代码专家（暖） | #D4A89A | 代码生成、沙箱执行 |
| **Assessment** | 评估智能体（中性） | #A8B5A0 | Quiz生成、评分反馈 |
| **Content Auditor** | 内容审核员（中性） | #B5A8A0 | 审核日志、拦截通知 |
| **Mentor** | 导师（冷） | #8FA3B5 | 问答响应、RAG过程 |


## 第三部分：页面架构与布局

### 3.1 全局布局结构（桌面端 ≥1024px）

```
┌──────────────────────────────────────────────────────────────────────────┐
│ Sidebar (240px)    │              Main Content Area                    │
│ ┌────────────────┐ │ ┌────────────────────────────────────────────────┐ │
│ │ Logo + 状态    │ │ │ 多课程标签栏 (自动隐藏，仅下拉切换)           │ │
│ ├────────────────┤ │ ├────────────────────────────────────────────────┤ │
│ │ 课程选择器▼    │ │ │                                                │ │
│ │ 进度条 68%     │ │ │         可定制工作台                          │ │
│ │ [＋上传资料]   │ │ │    ┌──────────┬──────────┬──────────┐        │ │
│ ├────────────────┤ │ │    │ 对话流   │ 资源/图谱 │ 画像卡片 │        │ │
│ │ 🗨️ 对话       │ │ │    │ (可拖拽  │ (可拖拽  │ (可拖拽  │        │ │
│ │ 🗺️ 图谱       │ │ │    │  调整    │  调整    │  调整)   │        │ │
│ │ 📚 资源库     │ │ │    └──────────┴──────────┴──────────┘        │ │
│ │ 📊 仪表盘     │ │ │                                                │ │
│ ├────────────────┤ │ ├────────────────────────────────────────────────┤ │
│ │ ⏳ 任务进度    │ │ │ [输入框]  [附件] [@工具]  [发送]              │ │
│ │ 🔔 通知 (3)   │ │ └────────────────────────────────────────────────┘ │
│ ├────────────────┤ │                                                   │
│ │ 👤 用户摘要    │ │                                                   │
│ └────────────────┘ └───────────────────────────────────────────────────┘
```

### 3.2 侧边栏设计规格

**展开状态（240px）**：

| 区域 | 高度 | 内容 |
|---|---|---|
| 品牌区 | 64px | Logo + "智图EduMap" + 状态指示点 |
| 课程上下文 | 100px | 下拉课程选择器、进度条 |
| 核心导航 | 240px | 4个导航项（图标+文字），当前项高亮 |
| 动态区 | 自适应 | 任务进度指示器（含迷你进度环）、通知铃铛 |
| 用户区 | 72px | 头像+姓名、画像标签、退出按钮 |

**折叠状态（56px）**：

| 元素 | 交互 |
|---|---|
| 品牌图标 | 点击展开侧边栏 |
| 导航图标 | 悬停显示Tooltip文字 |
| 进度环（智能叠加） | 右上角显示后台任务环形进度 |
| 用户头像 | 底部居中，点击弹出用户菜单 |
| 通知铃铛 | 折叠后移至底部用户头像上方 |

### 3.3 多课程标签栏（下拉切换模式）

```
┌─────────────────────────────────────────────┐
│  📘 当前: 人工智能导论  ▼  [进度 68%]      │  ← 点击展开下拉
├─────────────────────────────────────────────┤
│  ▼ 最近课程                                │
│    📘 人工智能导论  ● 68%  ✕              │
│    📗 数据结构        ● 45%  ✕              │
│    📕 机器学习        ● 23%  ✕              │
│    ────────────────────────                 │
│    📂 查看所有课程 →                        │
│    ＋ 添加新课程                            │
└─────────────────────────────────────────────┘
```

- 切换课程时：保存核心数据（对话+资源），视图状态重置为默认
- 课程关闭：下拉列表中显示"✕"删除按钮，数据移至"课程归档"


## 第四部分：核心页面/组件规格

### 4.1 空状态 - 首次登录（Onboarding引导）

**触发条件**：新用户首次登录，无任何课程、无对话历史、图谱为空。

**交互流程**：

```
步骤1: 全屏欢迎遮罩（带动画）
  ├─ 中央: "欢迎来到智图 EduMap" + 简洁插画
  ├─ 底部: "开始引导" 按钮
  └─ 跳过按钮（左下角小字）

步骤2: 引导走查 (3步)
  ├─ 高亮1: 侧边栏 "上传资料" 按钮
  │  └─ Tooltip: "上传你的课程资料，开启专属知识图谱"
  ├─ 高亮2: 主区域对话框
  │  └─ Tooltip: "通过对话，AI将了解你的学习风格和需求"
  └─ 高亮3: 空状态图谱区域
     └─ Tooltip: "上传资料后，知识点图谱将在这里自动生成"

步骤3: 引导完成
  └─ 显示 "开始使用" 按钮 → 进入主工作台（图谱区域仍为空，但显示引导式空状态插画）
```

**引导式空状态插画**（图谱区域）：

```
┌─────────────────────────────────────────────┐
│                                             │
│          🎨 [抽象几何插画 - 空书架]         │
│                                             │
│     "这里还空空的，开始你的学习旅程吧"      │
│                                             │
│        [📤 上传课程资料]   [💬 开始对话]    │
│                                             │
│      提示：上传PPT/PDF/DOCX，AI将为你       │
│      自动生成专属知识图谱                   │
└─────────────────────────────────────────────┘
```

### 4.2 资料上传工作流（资源库页面）

**触发方式**：
- 资源库页面（`/generate`）的"选择文件上传"按钮
- 支持拖拽/点击选择文件，单次上传一个文件
- 上传后自动触发文本解析和向量索引

**上传交互**：

```
┌──────────────────────────────────────────────────────────────────┐
│  📤 上传课程资料                              [✕ 最小化]       │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                                                          │ │
│  │      [拖拽文件到此处 或 点击选择文件]                    │ │
│  │                                                          │ │
│  │  支持格式: PDF / DOCX / PPT / MD  单个最大50MB          │ │
│  │  已选: 3个文件                                           │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  解析进度: ████████████░░░░░░░░ 60%  (3/5 文档完成)          │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ ✅ 人工智能导论_第1章.pdf   已完成  ████████ 100%        │ │
│  │      └─ 提取知识点: 12个 | 新增: 8个 | 合并: 4个        │ │
│  │ ⏳ 人工智能导论_第2章.docx  解析中... ██████░░ 70%       │ │
│  │      └─ 当前阶段: 知识点抽取...                          │ │
│  │ ⚠️ 补充材料_OCR版.pdf      需确认   ████████ 100%       │ │
│  │      └─ OCR识别率较低，部分公式可能不准确              │ │
│  │      └─ [查看详情] [跳过此文件]                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  当前解析已完成 3/5 文件，共提取 34 个知识点                   │
│                                                                  │
│                        [取消]  [后台处理]                        │
└──────────────────────────────────────────────────────────────────┘
```

**最小化后**：遮罩缩为侧边栏"解析任务进行中"指示器，用户可继续其他操作。

### 4.3 主对话区（工作台核心）

**布局**：占据工作台主区域（可拖拽调整宽度）

**对话消息类型**：

| 类型 | 样式 | 示例 |
|---|---|---|
| **用户消息** | 右对齐，品牌色气泡 | "帮我生成决策树的讲解文档" |
| **AI文字回复** | 左对齐，白色气泡，逐字打印 | 长文本Markdown渲染 |
| **AI资源卡片** | 左对齐，卡片样式 | 思维导图、习题、代码、图表 |
| **系统消息** | 居中，灰色背景，带标签 | "🔁 复习任务: 已启动反向传播Quiz" |
| **进度消息** | 左对齐，带Agent颜色标签 | "[Planner] 正在解析知识点..." |
| **错误消息** | 左对齐，橙色边框 | "生成失败，已返回低置信度版本" |

**输入框区域**：

```
┌──────────────────────────────────────────────────────────────────┐
│ [📎 附件] [@工具 ▼] [💬 输入你的问题或指令...]  [🔊]  [➤ 发送]│
│                                                                  │
│  提示: 支持上传图片/文件，@Designer生成资源，@Coder运行代码     │
└──────────────────────────────────────────────────────────────────┘
```

**@工具菜单**：

```
┌──────────────────────┐
│ 🔧 选择工具          │
│ ──────────────────── │
│ 📝 Designer 生成资源  │
│ 💻 Coder 运行代码     │
│ 📊 生成思维导图       │
│ 📝 生成练习题         │
│ 🎬 生成讲解视频(实验) │
│ ──────────────────── │
│ + 自定义指令...       │
└──────────────────────┘
```

### 4.4 对话历史侧边抽屉

**触发**：点击对话区左上角"📜 历史"按钮

```
┌─────────────────────────────────────────────┐
│  📜 对话历史                     [✕ 关闭]  │
│  [🔍 搜索会话...]                          │
│  ───────────────────────────────────────── │
│  📌 当前会话                              │
│    "决策树与随机森林对比"  今天 14:32     │
│  ───────────────────────────────────────── │
│  📂 历史会话                              │
│    "CNN卷积神经网络学习"   昨天 10:15     │
│      └─ 包含资源: 文档1  Quiz3            │
│    "反向传播算法复习"      前天 16:40     │
│      └─ 包含资源: 讲解文档  Mermaid1      │
│    "线性代数基础补充"      3天前 09:00    │
│    "第一章课后习题答疑"    5天前 20:15    │
│  ───────────────────────────────────────── │
│  [＋ 新建会话]  [🗑️ 管理历史]            │
└─────────────────────────────────────────────┘
```

**交互**：
- 点击历史会话 → 切换至该会话上下文（资源、图谱位置恢复）
- 悬停显示会话摘要Tooltip
- 支持批量删除、导出会话

### 4.5 资源展示区（标签页 + 模态框组合）

**主面板展示**（标签页切换）：

```
┌────────────────────────────────────────────────────────────────┐
│  📄 讲解文档  🧠 思维导图  📝 练习题  📊 图表  💻 代码     │  ← Tab栏
├────────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  📖 个性化讲解文档                    [📖 来源溯源]    │  │  ← 资源右上角
│  │  ────────────────────────────────────────────────────── │  │
│  │  personalization:                                     │  │  ← 可折叠Banner
│  │     rationale: 根据您的视觉型认知风格...               │  │
│  │     confidence: 0.94                                  │  │
│  │  ────────────────────────────────────────────────────── │  │
│  │                                                       │  │
│  │  ## 1. 决策树定义 [📖 源]                            │  │  ← 段落溯源锚点
│  │  决策树是一种监督学习算法，通过树形结构...           │  │
│  │  [悬停显示引用节点: 决策树定义(0.94) ...]            │  │  ← Popover
│  │                                                       │  │
│  │  ## 2. 信息增益计算 [📖 源]                          │  │
│  │  信息增益 = 熵(父) - Σ 熵(子) ...                    │  │
│  │                                                       │  │
│  │  ## 3. 剪枝策略 [📖 源]                              │  │
│  │  ...                                                 │  │
│  │                                                       │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                               │
│  📌 基于 15 个知识图谱节点生成  置信度 94%  ✅ 幻觉校验通过  │  ← 底部来源摘要
└────────────────────────────────────────────────────────────────┘
```

**全屏模态框**（点击"沉浸式查看"触发）：
- 用于深度交互场景：运行代码、完成习题、查看大图
- 背景遮罩，居中弹窗，支持ESC关闭
- 宽度 90%，高度 90%，圆角 16px

### 4.6 来源溯源 Popover（段落级）

**触发**：悬停段落右侧的"📖"图标（Q47-CE）

**规格**：

```
┌─────────────────────────────────────────────────────────┐
│  📖 知识来源 (本段落)                                  │
│  ────────────────────────────────────────────────────── │
│  引用知识图谱节点:                                      │
│                                                         │
│  ● 决策树定义 (n_001)                                  │
│    贡献: 核心定义来源  │  置信度: 0.94  │  ✅ 通过    │
│                                                         │
│  ● 监督学习分类 (n_002)                                │
│    贡献: 上下文关联    │  置信度: 0.87  │  ✅ 通过    │
│                                                         │
│  ● 信息增益公式 (n_005)                                │
│    贡献: 术语解释      │  置信度: 0.76  │  ⚠️ 校验弱 │
│                                                         │
│  ────────────────────────────────────────────────────── │
│  总体置信度: 0.89  │  幻觉校验: 通过                   │
│  [查看完整溯源报告]                                     │
└─────────────────────────────────────────────────────────┘
```

**数据来源**：`GET /resources/{resource_id}/provenance` 接口（设计见附录）

### 4.7 知识图谱画布（Skill-Tree-Canvas）

**默认视图**：全局图谱，DAG布局，前置关系清晰

**节点规格**：

| 属性 | 规格 |
|---|---|
| 形状 | 圆角矩形（知识点） / 六边形（章节） |
| 尺寸 | 120px × 48px（可缩放） |
| 颜色 | 掌握度映射（红/黄/绿），边界使用对应的莫兰迪化背景色 |
| 交互 | 悬停高亮 + 显示掌握度Tooltip / 点击聚焦 / 右键菜单 |
| 进度标识 | 节点右上角显示小进度环（0-100%） |
| 前置链 | 灰色箭头连线，点击节点时高亮路径 |

**聚焦模式（子图提取）**（Q18-B）：
- 点击节点 → 图谱重新布局，仅展示该节点相关的子图（前置+后置）
- 左上角显示"← 返回全局图谱"浮动按钮（毛玻璃效果）
- 子图边缘显示"关联节点数: 12"

**视图控制**：
- 缩放: Ctrl+滚轮
- 平移: 拖拽画布空白处
- 小地图: 右下角缩略图（可选折叠）
- 布局切换: 层次布局 / 力导向布局（工具栏）

### 4.8 任务看板（模态框 + 迷你浮窗） — ⚠️ 设计已废弃（对应组件已删除）

**完整模态框**（从侧边栏指示器或通知进入）：

```
┌──────────────────────────────────────────────────────────────────┐
│  ⚙️ 任务看板 - 资源生成                         [✕ 关闭]      │
│  ────────────────────────────────────────────────────────────── │
│  正在运行: 生成"决策树"讲解文档                                │
│  ████████████████████░░░░░░░░░░ 75%                           │
│  ────────────────────────────────────────────────────────────── │
│  多智能体协同流水线:                                           │
│                                                                  │
│  ✅ Orchestrator   任务路由完成            0.3s                │
│  ✅ Planner        知识点解析完成          1.2s                │
│     └─ 提取节点: 决策树定义, 信息增益, 剪枝策略...            │
│  ✅ Guardian       环检测通过              0.1s                │
│     └─ 难度单调性校验: 通过                                 │
│  ⏳ Designer       生成讲解文档中...       3.5s                │
│     └─ 当前: 生成思维导图...                                  │
│  ⬜ Coder          等待中                                     │
│  ⬜ Assessment     等待中                                     │
│  ⬜ Content Auditor 等待中                                    │
│  ────────────────────────────────────────────────────────────── │
│  [📊 查看中间产物]  [⏸ 暂停]  [✖ 取消]                     │
└──────────────────────────────────────────────────────────────────┘
```

**迷你浮窗**（后台运行时）：

```
┌────────────────────────────────────────────┐
│  ⚙️ 2个任务进行中  ████████░░ 65%       │  ← 聚合总进度
│  ──────────────────────────────────────── │
│  "决策树讲解文档"  ████████░░ 80%       │
│  最近: ✅ Designer 已完成思维导图         │
│  ──────────────────────────────────────── │
│  展开 ▼  取消                             │
└────────────────────────────────────────────┘
```

### 4.9 学习仪表盘

**布局**：三栏网格（可拖拽调整）

**Top 3 信息**（Q25-C）：遗忘曲线预警列表 + 推荐复习计划 + 学习活跃度

```
┌──────────────────────────────────────────────────────────────────┐
│  📊 学习仪表盘                                                  │
│  ┌───────────────────┐ ┌───────────────────┐ ┌───────────────┐ │
│  │ ⏰ 遗忘曲线预警   │ │ 📋 推荐复习计划   │ │ 🔥 学习活跃度 │ │
│  │ ───────────────── │ │ ───────────────── │ │ ───────────── │ │
│  │ 最高优先级:       │ │ 按优先级排序:     │ │ 本周学习时长  │ │
│  │ 🔴 反向传播算法  │ │ 1. 反向传播 [▶]  │ │ 12.5h        │ │
│  │    遗忘度 72%    │ │ 2. CNN池化层 [▶]  │ │ 较上周 +23%  │ │
│  │ 🟡 梯度消失问题  │ │ 3. 激活函数 [▶]   │ │              │ │
│  │    遗忘度 58%    │ │ 4. 正则化 [▶]     │ │ 日历热力图    │ │
│  │ 🟢 线性回归     │ │ [拖拽调整顺序]    │ │ ████░░░░░░   │ │
│  │    遗忘度 35%    │ │ [💾 保存] [↺重置] │ │ 每日连续学习  │ │
│  └───────────────────┘ └───────────────────┘ └───────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  🧠 画像雷达图 (6维)     📈 学习趋势    📊 掌握度分布    │ │
│  │      [雷达图]              [折线图]       [环形图]        │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

**复习计划交互**：
- 拖拽调整优先级 → 列表顶部出现"保存/重置"操作栏
- 点击"▶"一键启动复习会话 → 主对话区新建独立"复习会话"标签页

### 4.10 用户设置页

**布局**：左侧二级导航 + 右侧内容区

```
┌──────────────────────────────────────────────────────────────────┐
│  ⚙️ 设置                                    [返回工作台]       │
│  ┌─────────────┐ ┌────────────────────────────────────────────┐ │
│  │ 个人信息     │ │  🧑 个人信息                             │ │
│  │ 通知偏好     │ │  头像: [O]  姓名: 张三                   │ │
│  │ 课程管理     │ │  学校: XX大学  专业: 计算机科学与技术    │ │
│  │ 学习偏好     │ │  ──────────────────────────────────────── │ │
│  │ 隐私与数据   │ │  🔔 通知偏好                             │ │
│  │ 关于         │ │  ☑ 资源生成完成   ☑ 生成失败/需介入     │ │
│  └─────────────┘ │  ☑ Quiz复习提醒   ☑ 系统公告           │ │
│                   │  ☑ 桌面通知                             │ │
│                   │  ──────────────────────────────────────── │ │
│                   │  📚 课程管理                             │ │
│                   │  人工智能导论  ● 68%  [归档] [删除]   │ │
│                   │  数据结构       ● 45%  [归档] [删除]   │ │
│                   │  ──────────────────────────────────────── │ │
│                   │  🎯 学习偏好 (校正画像)                   │ │
│                   │  认知风格: 视觉型 → [▼ 下拉调整]         │ │
│                   │  数学基础: 0.7 → [滑块调整]              │ │
│                   │  ──────────────────────────────────────── │ │
│                   │  🔒 隐私与数据                           │ │
│                   │  [导出所有数据]  [注销账号]              │ │
│                   └────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

### 4.11 演示控制台（Progress Slider + 模拟学习周）

**位置**：skill-tree-canvas 底部，固定工具栏

```
┌──────────────────────────────────────────────────────────────────┐
│  🎯 学习进度模拟  [⚡ 一键模拟学习周]                          │
│  0% ──●────────────────────────────── 100%                     │
│  第3天  ████████░░░░ 掌握度 45%                                │
│  当前阶段: 学习"决策树剪枝策略"                                │
│  ▶ 播放 (1x)  ⏸ 暂停  ↺ 重置                                 │
│  速度: [0.5x] [1x] [2x] [4x]                                  │
│  ────────────────────────────────────────────────────────────── │
│  联动面板: [🔄 画像雷达图] [📈 遗忘曲线] [🗺️ 图谱颜色] 同步更新│
└──────────────────────────────────────────────────────────────────┘
```

**播放行为**（Q46-BF）：
- 默认速度: 1天/2秒（7天在14秒内完成）
- 快速拖拽: 压缩播放中间帧，0.5秒内完成快进过渡
- 每10%触发一次图谱重绘 + 画像雷达图动画 + 遗忘曲线更新
- 播放过程中，右下角显示"Day 3/7"计时器


## 第五部分：响应式策略

### 5.1 断点定义

| 断点 | 宽度 | 布局模式 |
|---|---|---|
| Desktop | ≥1024px | 完整侧边栏 + 可定制工作台 |
| Tablet | 640px ~ 1023px | 侧边栏可折叠（默认折叠），保留部分信息 |
| Mobile | <640px | 底部Tab Bar + 全屏内容堆叠 |

### 5.2 移动端底部Tab Bar（<640px）

```
┌──────────────────────────────────────────────────┐
│  主内容区域 (全屏，堆叠显示)                      │
│  ┌────────────────────────────────────────────┐  │
│  │  对话流 (默认显示)                         │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │  资源/图谱 (滑动切换)                      │  │
│  └────────────────────────────────────────────┘  │
│                                                  │
│  ┌──────────┬──────────┬──────────┬──────────┐  │
│  │ 🗨️ 对话  │ 🗺️ 图谱  │ 📚 资源  │ ⚙️ 更多 │  │  ← Tab Bar (56px)
│  └──────────┴──────────┴──────────┴──────────┘  │
└──────────────────────────────────────────────────┘
```

**"更多"菜单**：
```
┌──────────────────────┐
│  📊 仪表盘            │
│  📜 对话历史          │
│  ⚙️ 设置              │
│  👤 个人画像          │
└──────────────────────┘
```

### 5.3 移动端特殊适配

| 组件 | 桌面端 | 移动端 |
|---|---|---|
| 技能树 | 完整交互（缩放/平移/聚焦） | 简化为只读视图（可缩放，无拖拽定制） |
| 资源生成 | 任务看板模态框 | 底部抽屉（Bottom Sheet） |
| 输入框 | 固定底部 | 跟随键盘，自动上浮 |
| 多课程切换 | 侧边栏下拉 | Tab Bar"更多"菜单内 |


## 第六部分：关键交互流程

### 6.1 新用户完整引导流程

```
开始 → 注册登录
  → 全屏欢迎遮罩（插画+标题）
  → 点击"开始引导"
  → Step1: 高亮"上传资料"（Tooltip: "上传课程资料，开启专属知识图谱"）
  → Step2: 高亮"对话输入框"（Tooltip: "通过对话，AI将了解你的学习风格"）
  → Step3: 高亮"空图谱区域"（Tooltip: "上传资料后，图谱将自动生成"）
  → 点击"开始使用"
  → 进入主工作台（图谱区显示空状态引导插画）
  → 用户点击"上传课程资料"或"开始对话"
  → 引导完成，进入自由使用模式
```

### 6.2 资料上传 → 图谱生成 → 开始学习 全链路

```
用户点击"上传资料"
  → 全屏遮罩弹出（拖拽/点击选择文件，支持批量）
  → 文件上传 → 依次显示每个文档解析进度
  → Planner Agent 解析 → 提取知识点 → 语义去重（ChromaDB相似度>0.9合并）
  → 新增节点写入Neo4j → 图谱更新
  → 解析完成摘要卡片（"共提取47个知识点，新增35个"）
  → 用户点击"进入学习"
  → 遮罩关闭，图谱区域显示完整知识图谱
  → 侧边栏课程进度更新
  → 对话区自动发送系统消息: "✅ 已成功解析《人工智能导论》，共47个知识点，开始你的学习吧！"
  → 用户可开始对话请求资源生成
```

### 6.3 资源生成 → 任务看板 → 审核 → 交付 全链路

```
用户在对话中输入: "帮我生成决策树的讲解文档"
  → Orchestrator 接收任务，路由至 Planner
  → 侧边栏显示活跃任务指示器（旋转加载图标 + "生成决策树讲解文档..."）
  → 自动弹出任务看板模态框（半透明）
  → 看板实时更新各Agent状态:
     - Planner: ✅ 解析完成 (1.2s)
     - Guardian: ✅ 环检测通过 (0.1s)
     - Designer: ⏳ 生成讲解文档中... (3.5s)
     - Content Auditor: ⬜ 等待中
     - Coder: ⬜ 等待中
     - Assessment: ⬜ 等待中
  → Designer 完成 → 提交 Content Auditor
  → Auditor: 向量相似度 0.87 > 0.8 ✅ 通过
  → Assessment: 生成配套 Quiz (1-3题)
  → 全部完成 → 看板显示 "✅ 全部完成！"
  → 资源自动推送到主对话区（讲解文档 + 思维导图 + Quiz）
  → 侧边栏通知铃铛红点 +1
  → 浏览器桌面通知: "✅ 决策树讲解文档已生成！"
  → 用户关闭看板（或点击"查看资源"）
  → 对话区展示资源卡片（含个人化Banner）
```

**异常分支**：Content Auditor 相似度 < 0.8
```
→ Auditor: ❌ 相似度 0.72 < 0.8，触发重做 (第1次)
→ Designer 重新生成 → Auditor: 0.75 < 0.8 (第2次)
→ Designer 重新生成 → Auditor: 0.68 < 0.8 (第3次)
→ 连续3次失败 → 降级返回最佳版本（0.75）+ 低置信度标志
→ 看板显示 "⚠️ 生成完成，置信度较低 (0.75)"
→ 资源卡片显示黄色横幅: "本内容置信度较低，建议补充资料后重新生成"
```

### 6.4 复习闭环流程

```
仪表盘 → 遗忘曲线预警列表 → 用户点击"▶ 开始复习"
  → 对话区新建"复习会话"标签页（独立于主对话）
  → 自动发送系统消息卡片: 
     "🔁 复习任务: 反向传播算法 (遗忘度72%)
      ⏱ 预计完成: 2分钟 | 📝 1-3道选择题"
  → 系统自动加载 Quiz 题目（由 Assessment Agent 生成）
  → 用户逐题作答
  → 提交后显示评分 + 解析
  → 更新 mastery_level → 更新遗忘曲线参数 r
  → 更新画像权重（全局累加）
  → 关闭复习会话，返回主对话
  → 仪表盘复习列表更新（该知识点标记为"已复习"）
```

### 6.5 全局错误处理（混合策略）

```
┌─────────────────────────────────────────────────────────────────┐
│ 网络级错误（断网/API超时/服务不可达）                          │
│ → 顶部全局横幅: "⚠️ 网络连接异常，部分功能暂不可用"           │
│    [手动重试] [查看详情]                                      │
│ → 所有页面可见，直到网络恢复                                  │
├─────────────────────────────────────────────────────────────────┤
│ 业务级错误（资源生成失败/审核不通过）                          │
│ → 对话内系统消息卡片: "❌ 生成失败，已返回低置信度版本"       │
│    展示失败原因 + 重试按钮 + 上传补充资料引导                 │
│ → 不影响其他功能使用                                          │
├─────────────────────────────────────────────────────────────────┤
│ 冷启动阻断（置信度 < 0.4）                                    │
│ → 全屏遮罩: "📚 当前主题语料稀缺，请先上传参考文档"           │
│    展示当前置信度: 0.28 + 上传按钮                            │
│ → 上传后增量增强，完成后自动恢复                              │
└─────────────────────────────────────────────────────────────────┘
```


## 第七部分：接口契约清单

### 7.1 核心接口

| 接口 | 方法 | 说明 | 调用场景 |
|---|---|---|---|
| `/api/v1/resources/upload` | POST | 上传文件（PDF/MD/DOCX/PPT/TXT/代码） | 资源库上传 |
| `/api/v1/resources` | GET | 资源列表（支持 user_id/type/source 筛选） | 资源库展示 |
| `/api/v1/resources/{id}` | DELETE | 删除资源 | 资源库删除 |
| `/api/v1/orchestrator/generate` | POST | 触发多智能体资源生成管线 | 对话生成指令 |
| `/api/v1/orchestrator/stream/{session_id}` | GET (SSE) | 实时获取生成管线进度 | 任务看板 |
| `/api/v1/orchestrator/status/{session_id}` | GET | 查询生成任务状态 | 任务进度轮询 |
| `/api/v1/mentor/stream/{user_id}` | GET (SSE) | RAG 辅导问答 | 对话辅导模式 |
| `/api/v1/kg/courses/{courseId}/graph` | GET | 知识图谱数据 | 图谱画布 |
| `/api/v1/learning-path/{courseId}` | GET | 个性化学习路径 | 学习路径页 |
| `/api/v1/learning-path/progress` | PUT | 更新学习进度 | 进度同步 |

### 7.2 来源溯源接口详细设计（Q回答）

**请求**：
```
GET /api/resources/{resource_id}/provenance?preload=true
```

**响应**：
```json
{
  "resource_id": "res_abc123",
  "overall_confidence": 0.91,
  "hallucination_check": "passed",
  "generated_at": "2026-07-13T14:32:00Z",
  "paragraphs": [
    {
      "paragraph_id": "p1",
      "start_char": 0,
      "end_char": 120,
      "text_preview": "决策树是一种监督学习算法，通过树形结构...",
      "referenced_nodes": [
        {
          "node_id": "n_001",
          "node_name": "决策树定义",
          "node_category": "核心概念",
          "confidence": 0.94,
          "contribution": "核心定义来源",
          "hallucination_check": "passed"
        },
        {
          "node_id": "n_002",
          "node_name": "监督学习分类",
          "node_category": "上下文关联",
          "confidence": 0.87,
          "contribution": "上下文关联",
          "hallucination_check": "passed"
        }
      ]
    }
  ],
  "total_nodes_referenced": 15
}
```


## 第八部分：开发优先级与里程碑

| 阶段 | 核心交付 | 预估工时 |
|---|---|---|
| **Phase 1 (骨架)** | 全局布局、侧边栏、路由、响应式断点 | 5天 |
| **Phase 2 (对话)** | 对话区、流式渲染、历史列表、输入框工具 | 8天 |
| **Phase 3 (图谱)** | D3.js技能树、聚焦模式、进度滑块、演示控制台 | 10天 |
| **Phase 4 (资源)** | 资源展示Tab、来源溯源Popover、任务看板、迷你浮窗 | 8天 |
| **Phase 5 (上传)** | 全屏遮罩、拖拽上传、解析进度、冷启动阻断 | 5天 |
| **Phase 6 (仪表盘)** | 雷达图、遗忘曲线、复习计划、拖拽排序 | 6天 |
| **Phase 7 (设置)** | 用户设置页、通知偏好、课程管理 | 4天 |
| **Phase 8 (集成)** | 接口联调、错误处理、性能优化、演示准备 | 7天 |
| **总计** | | **53天（约11周）** |

