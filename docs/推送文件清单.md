# 可推送远程仓库文件清单

> 远程：`https://github.com/Erd-omg/EduMap`（branch: `main`）

## 一、敏感信息处理说明（已确认安全）

| 项 | 状态 |
|---|---|
| `.env`（含真实 LLM_API_KEY） | ✅ **不被跟踪**（.gitignore 第 14 行），不会推送 |
| `.venv/`（1.7GB 虚拟环境） | ✅ **已加入 .gitignore**，不会推送 |
| `services/backend-core/uploads/`（运行时上传文件） | ✅ **已加入 .gitignore**，不会推送 |
| `services/backend-core/.coverage`（测试产物） | ✅ **已加入 .gitignore**，不会推送 |
| `.env.example`（模板） | ✅ 空密钥占位，安全推送 |
| 全推集密钥扫描（sk-*/AKIA/私钥/密码） | ✅ 0 命中 |

**加密/脱敏建议**：本项目凭 API Key 走 .env 注入，git 侧无需加密；若后续要把密钥存仓库，请用 `git secret` 或 `sops`，但当前无需。

## 二、可推送文件（~159 个变更）

### 1. 后端核心代码（`services/backend-core/`，Phase 6-8 + 本次修复）
- `src/harness/`（BaseAgent/Retry/StructuredOutput/Mixins）
- `src/memory/`（三层记忆系统）
- `src/tools/`（工具注册系统）
- `src/rag/`（chunking/reranking/evaluation + rag_service/router）
- `src/privacy/`（PIPL 合规导出）
- `src/analysis/`（画像分析）
- `src/agents/`（mentor/assessment/planner/designer/coder/content_auditor 修复）
- `src/resources/`（repository/router/parser/models — created_at/id 修复）
- `src/learning_path/forgetting_curve.py`、`src/auth.py`、`src/logging_middleware.py`
- `src/agents/orchestrator/`（router.py — 超时 + 状态传播修复）
- `tests/`（614 项测试全通过）
- `pytest.ini`、`scripts/`（开发工具脚本）
- `benchmark_results/`（RAG 评测结果，README 引用）

### 2. 后端服务（profile/sandbox）
- `services/profile-service/src/auth.py`、`logging_middleware.py`
- `services/sandbox-service/`（Dockerfile + tests）
- `scripts/db/init/03-memory-schema.sql`（记忆系统表迁移）

### 3. 基础设施修复（docker-compose + Dockerfile）
- `docker-compose.yml`（build context 修复、chroma/backend healthcheck 修复、HF 缓存卷）
- `services/backend-core/Dockerfile`（COPY 路径 + uv 超时）
- `services/backend-core/requirements.txt`

### 4. 前端（`apps/web/`）
- `src/app/`（dashboard/settings/error 新页面 + 各页测试）
- `src/components/`（layout/cold-start/dashboard/knowledge-graph/profiling/quiz/resources/ui/onboarding/provenance）
- `src/hooks/`、`src/stores/`、`src/lib/`（+ 各 __tests__）
- `src/test/`、`e2e/`、`playwright.config.ts`、`vitest.config.ts`

### 5. 文档与杂项
- `docs/手动测试清单.md`（**新增**，实机测试清单）
- `docs/推送文件清单.md`（**新增**，本文档）
- `reference_doc/`（架构/代码走读/访谈指南）
- `frontend-design.md`、`gap-analysis.md`、`test-report.md`
- `README.md`、`PRD.md`、`.env.example`、`.gitignore`

## 三、不推送（已排除）

`.env` / `.env.local` / `.venv/` / `node_modules/` / `.next/` / `uploads/` / `.coverage` / `__pycache__/` / `*.log`

## 四、推送步骤（待执行）

```bash
git add -A
git commit -m "feat: Phase 6-8 (记忆系统/RAG增强/Harness) + 运维修复 + 测试文档"
git push -u origin main
```

> ⚠️ 推送前需 GitHub 凭据（HTTPS token 或 SSH）。当前仓库无提交历史冲突风险（全新 remote）。
