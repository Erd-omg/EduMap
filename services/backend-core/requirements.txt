fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0
langchain>=0.3.0
langgraph>=0.2.0
# Durable graph state (checkpointer). The [binary] psycopg variant bundles
# libpq so no system PostgreSQL client library is required — the project
# otherwise uses asyncpg, which needs nothing installed.
#
# NOTE: this file and pyproject.toml must be kept in sync. CI and the Dockerfile
# install from *this* file, so a dependency added only to pyproject.toml is
# absent at test/build time.
langgraph-checkpoint-postgres>=3.0.0
psycopg[binary]>=3.2.0
neo4j>=5.20.0
# ChromaDB — pinned to 1.5.x to match server version (chromadb/chroma:1.5.9).
chromadb>=1.5.0,<1.6.0
sentence-transformers>=3.0.0
redis>=5.0.0
asyncpg>=0.29.0
numpy>=1.24.0
celery>=5.4.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
unstructured[pdf,docx,pptx]>=0.14.0
httpx>=0.27.0
sse-starlette>=2.1.0
jieba>=0.42.1
